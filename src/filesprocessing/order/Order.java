package filesprocessing.order;

import java.io.File;
import java.util.Comparator;

/** An Order Abstract class, that implements a comparator. */
public abstract class Order implements Comparator<File> {

}
